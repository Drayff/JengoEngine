namespace JengoEngine.Collider
{
    public class Collider
    {
        public bool CollidesWith(Collider other)
        {
            // Логика для проверки столкновения. Идеи: В объектах хранить размер, который будет влиять на функцию GetGrans, и сравнивать координаты.
            // Или проверять расстояния между центрами объектов, радиусы и т.д.
            // true, если столкновение произошло, иначе false.
            return false;
        }
    }
}
